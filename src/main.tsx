import React from "react";
import ReactDOM from "react-dom/client";
import TekSpherePage from "./TekSpherePage";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <TekSpherePage />
  </React.StrictMode>,
);
